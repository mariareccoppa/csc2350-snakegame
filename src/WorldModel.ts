import display from "./display";
import Point from "./Point";
import Snake from "./Snake";

class WorldModel {
  private sna: Snake;
  constructor(s: Snake) {
    this.sna = s;
  }
  set Snake(s: Snake) {
    this.sna = s;
  }
  public update(steps:)
}

export default WorldModel;
